// FILE: top_ope_b_rep_ds_export.rs
// occt: TopOpeBRepDS_EXPORT

// This is a header-only export definitions file in C++
// In Rust, we use visibility modifiers instead
// This module contains no types or functions, as it's just a marker for C++ export macros

#[cfg(test)]
mod tests {
    #[test]
    fn test_export_module() {
        // Export module is for C++ compatibility only
        assert!(true);
    }
}
