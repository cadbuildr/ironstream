# Modules removed from crates/ironstream/src because they failed to build or
# failed their ported OCCT tests, queued for honest fixes. Not counted by
# parity.py (a removed module carries no `// occt:` marker).
#
# Format: <module_stem> <OCCT_Class> <build-error|test-fail>
#
# Invariant, enforced by parity/guardrails.py: a module listed here must NOT
# exist in src/. When a quarantined module is fixed and re-integrated, delete
# its line. (The previous 63 entries were all fixed and re-integrated in later
# rescue rounds but never removed from this list; cleared 2026-07 during the
# audit remediation.)
#
# The backlog is currently EMPTY — every ported module in src builds and its
# ported tests pass. Test modules excluded from the suite are tracked
# separately in parity/suite_exclusions.txt.
